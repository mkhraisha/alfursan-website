# Allow packages with stale peer dependency declarations to install cleanly.
# Needed because @astrojs/check@0.9.8 declares peerDep typescript@^5 but works
# correctly with typescript@6. Remove this once @astrojs/check ships TS6 support.
legacy-peer-deps=true
